name: Deploy production

'on':
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production deployment is paused until the validated artifact workflow is enabled."
